package main
import (
    "fmt"
)
func main () {
 var n int
    var name string
    
    fmt.Scan (&n, &name)
    if n > 11 && n < 19 && name == "m"{
        fmt.Println("YES")
   }else{
       fmt.Println("NO")
      
    }
    
    
}
